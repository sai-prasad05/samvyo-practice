# === NEVER COMMIT THESE ===
.env
.env.local
.env.production
*.pem
*.key

# Node — ignore node_modules anywhere in the project tree, not just root
node_modules/
**/node_modules/
dist/

# OS noise
.DS_Store
